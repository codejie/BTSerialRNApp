import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    main: {
        flex: 1,
        alignItems: 'center'
    },
    font_24: {
        fontSize: 24,
        fontWeight: 'bold',
        color: 'blue'
    }
});

export default styles;